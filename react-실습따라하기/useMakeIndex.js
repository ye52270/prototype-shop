import { useEffect } from 'react';

import useReduxState from './useReduxState';

export default function useMakeIndex() {
  // const aa = useReduxState();
  // const index = 0;
  //const state = useReduxState();
  // useEffect(() => {
  //   state.map((t) => t.index).reduce((pre, cur) => (pre > cur ? pre : cur), 0);
  // }, []);

  return 1;
}
